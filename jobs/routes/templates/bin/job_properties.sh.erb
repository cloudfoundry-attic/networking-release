#!/usr/bin/env bash

#
# Routes job properties
#

# Directory to store the Routes job configuration files
export ROUTES_CONF_DIR=${JOB_DIR}/config

# Directory to store the Routes job log files
export ROUTES_LOG_DIR=${LOG_DIR}

# Directory to store the Routes job process IDs
export ROUTES_PID_DIR=${RUN_DIR}

# Directory to store the Routes job data files
export ROUTES_STORE_DIR=${STORE_DIR}

# Directory to store the Routes job temp files
export ROUTES_TMP_DIR=${TMP_DIR}
